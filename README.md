# Pipedrive SDK

Pipedrive API v1 client, generated from the OpenAPI spec.

Learn more about Pipedrive API v1 at [pipedrive.com](https://pipedrive.com).

This is an unofficial SDK for the Pipedrive API v1 public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

Full documentation for this SDK: [https://voxgig-sdk.github.io/pipedrive-sdk/](https://voxgig-sdk.github.io/pipedrive-sdk/)

> TypeScript, Python, PHP, Golang, Lua, JavaScript SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

> **Features:** `debug`, `idempotency`, `metrics`, `paging`, `ratelimit`, `retry`, `test`, `timeout` — opt-in,
> inactive until switched on, and configured per client. See the Features
> section of any SDK README below for what each one does.

## Entities, not endpoints

This SDK exposes the API as **42 semantic entities** that you
call directly, instead of assembling URL paths and query strings. See the [Entities](#entities) table below for the full list. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`, `create`, `update`, `remove`):

```ts
const client = new PipedriveSDK()
const items = await client.ActivityField().list()
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = PipedriveSDK.test({
  entity: {
    mailbox: {
      test01: { id: 'test01' },
    },
  },
})
const mailboxs = await client.Mailbox().list()
// mailboxs is an array of Mailbox entities, populated with mock data
// — call mailboxs[0].data() for the record itself
console.log(mailboxs)
```

### Python

```python
client = PipedriveSDK.test()
mailboxs = client.Mailbox().list()
print(mailboxs)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = PipedriveSDK::test([
    "entity" => ["mailbox" => ["test01" => ["id" => "test01"]]],
]);
$mailboxs = $client->Mailbox()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.Mailbox(nil).List(
    nil, nil,
)
```

### Lua

```lua
local client = sdk.test()
local results, err = client:Mailbox():list()
```

### JavaScript

```js
const client = PipedriveSDK.test()
const mailboxs = await client.Mailbox().list()
// mailboxs is an array of entities, populated with mock data
// — call mailboxs[0].data() for the record itself
console.log(mailboxs)
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/pipedrive` | publish pending — [install from git tag](https://github.com/voxgig-sdk/pipedrive-sdk/tags) |
| Python | `voxgig-sdk-pipedrive` | publish pending — [install from git tag](https://github.com/voxgig-sdk/pipedrive-sdk/tags) |
| PHP | `voxgig-sdk/pipedrive` | publish pending — [install from git tag](https://github.com/voxgig-sdk/pipedrive-sdk/tags) |
| Golang | `github.com/voxgig-sdk/pipedrive-sdk/go` | `go get github.com/voxgig-sdk/pipedrive-sdk/go@latest` |
| Lua | `voxgig-sdk-pipedrive` | publish pending — [install from git tag](https://github.com/voxgig-sdk/pipedrive-sdk/tags) |
| JavaScript | `@voxgig-sdk/pipedrive-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/pipedrive-sdk/tags) |
| Go CLI | `github.com/voxgig-sdk/pipedrive-sdk/go-cli` | `go install github.com/voxgig-sdk/pipedrive-sdk/go-cli/cmd/pipedrive@latest` |
| Go MCP server | `github.com/voxgig-sdk/pipedrive-sdk/go-mcp` | `go get github.com/voxgig-sdk/pipedrive-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { PipedriveSDK } from '@voxgig-sdk/pipedrive'

const client = new PipedriveSDK({
  apikey: process.env.PIPEDRIVE_APIKEY,
  secret: process.env.PIPEDRIVE_SECRET,
})

// List all activityfields (returns ActivityFieldEntity[] — .data() for the record)
const activityfields = await client.ActivityField().list()
for (const activityfield of activityfields) {
  console.log(activityfield)
}

// Load a specific deal (returns a Deal)
const deal = await client.Deal().load({
  amount: 1,
  field_key: 'example_field_key',
  interval: 'example_interval',
  start_date: 'example_start_date',
})
console.log(deal)
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Lua, JavaScript) | `ts/` `py/` `php/` `go/` `lua/` `js/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o pipedrive-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "pipedrive": {
      "command": "/abs/path/to/pipedrive-mcp"
    }
  }
}
```

## Entities

The API exposes 42 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **ActivityField** | The ActivityField entity (list). | `/activityFields` |
| **ActivityType** | The ActivityType entity (create, list, remove, update). | `/activityTypes` |
| **Billing** | The Billing entity (list). | `/billing/subscriptions/addons` |
| **CallLog** | The CallLog entity (create, list, load, remove). | `/callLogs` |
| **Channel** | The Channel entity (create, remove). | `/channels` |
| **Currency** | The Currency entity (list). | `/currencies` |
| **Deal** | The Deal entity (create, list, load, remove, update). | `/deals/archived` |
| **DealField** | The DealField entity (create, list, load, remove, update). | `/dealFields` |
| **File** | The File entity (create, list, load, remove, update). | `/files` |
| **Filter** | The Filter entity (create, list, load, remove, update). | `/filters` |
| **Goal** | The Goal entity (create, load, remove, update). | `/goals/find` |
| **Lead** | The Lead entity (create, list, load, remove, update). | `/leads` |
| **LeadField** | The LeadField entity (list). | `/leadFields` |
| **LeadLabel** | The LeadLabel entity (create, list, remove, update). | `/leadLabels` |
| **LeadSource** | The LeadSource entity (list). | `/leadSources` |
| **LegacyTeam** | The LegacyTeam entity (create, list, load, remove, update). | `/legacyTeams` |
| **Mailbox** | The Mailbox entity (list, load, remove, update). | `/mailbox/mailThreads/{id}/mailMessages` |
| **Meeting** | The Meeting entity (create, remove). | `/meetings/userProviderLinks` |
| **Note** | The Note entity (create, list, load, remove, update). | `/notes` |
| **NoteField** | The NoteField entity (list). | `/noteFields` |
| **Oauth** | The Oauth entity (create, load). | `/oauth/authorize` |
| **Organization** | The Organization entity (create, list, remove, update). | `/organizations/{id}/flow` |
| **OrganizationField** | The OrganizationField entity (create, list, load, remove, update). | `/organizationFields` |
| **OrganizationRelationship** | The OrganizationRelationship entity (create, list, load, remove, update). | `/organizationRelationships` |
| **PermissionSet** | The PermissionSet entity (list, load). | `/permissionSets` |
| **Person** | The Person entity (create, list, remove, update). | `/persons/{id}/flow` |
| **PersonField** | The PersonField entity (create, list, load, remove, update). | `/personFields` |
| **Pipeline** | The Pipeline entity (list, load). | `/pipelines/{id}/deals` |
| **Product** | The Product entity (create, list, remove). | `/products/{id}/deals` |
| **ProductField** | The ProductField entity (create, list, load, remove, update). | `/productFields` |
| **Project** | The Project entity (create, list, load, remove, update). | `/projects` |
| **ProjectBoard** | The ProjectBoard entity (list, load). | `/projects/boards` |
| **ProjectPhase** | The ProjectPhase entity (list, load). | `/projects/phases` |
| **ProjectTemplate** | The ProjectTemplate entity (list, load). | `/projectTemplates` |
| **Recent** | The Recent entity (list). | `/recents` |
| **Role** | The Role entity (create, list, load, remove, update). | `/roles` |
| **Stage** | The Stage entity (list). | `/stages/{id}/deals` |
| **Task** | The Task entity (create, list, load, remove, update). | `/tasks` |
| **User** | The User entity (create, list, load, update). | `/users` |
| **UserConnection** | The UserConnection entity (load). | `/userConnections` |
| **UserSetting** | The UserSetting entity (load). | `/userSettings` |
| **Webhook** | The Webhook entity (create, list, remove). | `/webhooks` |

The operations available across these entities are **load**, **list**, **create**, **update**, **remove** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from pipedrive_sdk import PipedriveSDK

client = PipedriveSDK({
    "apikey": os.environ.get("PIPEDRIVE_APIKEY"),
})

# List all activityfields (returns a list, raises on error)
activityfields = client.ActivityField().list()
for activityfield in activityfields:
    print(activityfield)
```

### PHP

```php
<?php
require_once 'pipedrive_sdk.php';

$client = new PipedriveSDK([
    "apikey" => getenv("PIPEDRIVE_APIKEY"),
]);

// List all activityfields (returns an array; throws on error)
$activityfields = $client->ActivityField()->list();
print_r(array_map(fn($item) => $item->data_get(), $activityfields));
```

### Golang

```go
import sdk "github.com/voxgig-sdk/pipedrive-sdk/go"

client := sdk.NewPipedriveSDK(map[string]any{
    "apikey": os.Getenv("PIPEDRIVE_APIKEY"),
})

// List all activityfields
activityFields, err := client.ActivityField(nil).List(nil, nil)
if err != nil {
    panic(err)
}
fmt.Println(activityFields)

// Load a specific deal
deal, err := client.Deal(nil).Load(
    map[string]any{"amount": 1, "field_key": "example_field_key", "interval": "example_interval", "start_date": "example_start_date"}, nil,
)
if err != nil {
    panic(err)
}
fmt.Println(deal)
```

### Lua

```lua
local sdk = require("pipedrive_sdk")

local client = sdk.new({
  apikey = os.getenv("PIPEDRIVE_APIKEY"),
})

-- List all activityfields
local activityfields, err = client:ActivityField():list()
print(activityfields)
```

### JavaScript

```js
const { PipedriveSDK } = require('@voxgig-sdk/pipedrive-js')

const client = new PipedriveSDK({
  apikey: process.env.PIPEDRIVE_APIKEY,
})

// List all activityfields (returns an array)
const activityfields = await client.ActivityField().list()
for (const activityfield of activityfields) {
  console.log(activityfield)
}

// Load a specific deal (returns the entity)
const deal = await client.Deal().load({
  amount: 1,
  field_key: 'example_field_key',
  interval: 'example_interval',
  start_date: 'example_start_date',
})
console.log(deal)
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **DebugFeature** | Request/response capture ring buffer for debugging |
| **IdempotencyFeature** | Idempotency keys for safe retries of mutating operations |
| **MetricsFeature** | Statistics capture: per-operation counters and latency |
| **PagingFeature** | Pagination signals for list operations |
| **RatelimitFeature** | Client-side rate limiting via a token bucket |
| **RetryFeature** | Automatic retry of transient failures with exponential backoff |
| **TestFeature** | In-memory mock transport for testing without a live server |
| **TimeoutFeature** | Per-request timeout with transport abort |

Pass custom features via the `extend` option at construction time.

## Customizing this SDK

This repository contains its own generator (`.sdk/`), so the SDK is
customizable without forking any upstream tool:

- **The model** (`.sdk/model/`) declares everything this project owns:
  package names, versions, active features, per-target settings. It is
  written in [aontu](https://aontu.dev), a JSON-based
  specification language designed for building ontologies: easy to edit
  by hand, and files unify rather than override, so small declarations
  compose into one model. Regeneration re-reads it every time.
- **Templates** (`.sdk/tm/`) and **components** (`.sdk/src/cmp/`) are
  the two layers of generation, copied into this repo: templates are the
  literal per-language source, components generate the API-shaped parts.
- **Regeneration merges.** By default, newly generated content is
  three-way merged into existing files, so generator updates and local
  edits usually converge without manual conflict handling. A project can
  opt for plain overwrite instead.
- **Custom features and entire custom targets** arrive through sdkgen
  packages (`voxgig-sdkgen package add`), on the same rails as the
  bundled languages, and `voxgig-sdkgen doctor` reports any drift from
  what a resync would write.

How-to: [customize and propagate templates](https://github.com/voxgig/sdkgen/blob/main/docs/how-to/customize-and-propagate-templates.md).
The full story: [voxgig.com/sdk/custom](https://voxgig.com/sdk/custom).

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Lua](lua/README.md)
- [JavaScript](js/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [https://api.pipedrive.com/v1](https://api.pipedrive.com/v1)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Pipedrive API v1 OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
