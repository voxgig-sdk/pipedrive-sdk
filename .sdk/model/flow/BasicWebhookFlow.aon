# BasicWebhookFlow

main: kit: flow: BasicWebhookFlow:
{
  entity: "webhook",
  kind: "basic",
  name: "BasicWebhookFlow",
  step: [
    {
      input: {
        ref: "webhook_ref01"
      }

      op: "create"
    }

    {
      op: "list",
      valid: [
        {
          apply: "ItemExists",
          def: {
            ref: "webhook_ref01"
          }
        }
      ]
    }

    {
      input: {
        ref: "webhook_ref01",
        suffix: "_rm0"
      }

      match: {
        id: "webhook01"
      }

      op: "remove"
    }

    {
      input: {
        suffix: "_rt0"
      }

      op: "list",
      valid: [
        {
          apply: "ItemNotExists",
          def: {
            ref: "webhook_ref01"
          }
        }
      ]
    }
  ],
  "key$": "BasicWebhookFlow"
}